# URL de l'API Strapi (avec le préfixe /api)
BASE_URL_API=http://localhost:1337/api

# Token d'API Strapi (Settings > API Tokens, type "Read-only" suffisant)
# Utilisé uniquement au build, jamais exposé côté client.
TOKEN_API=tobemodified
