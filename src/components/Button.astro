---
interface Props {
  variant?: "filled" | "outline";
  href?: string;
}
const { variant = "filled", href } = Astro.props;
const Tag = href ? "a" : "button";
---

<Tag href={href} class:list={["btn", `btn--${variant}`]}>
  <slot />
</Tag>
