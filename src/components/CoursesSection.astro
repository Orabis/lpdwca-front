---
import SectionBase from "./SectionBase.astro";
import CourseCard from "./CourseCard.astro";

interface Course {
  id: number;
  title: string;
  description: string;
  imageSrc?: string;
  imageAlt?: string;
  href?: string;
}

interface Props {
  courses: Course[];
  intro?: string;
  actionHref?: string;
}

const { courses, intro, actionHref = "/formation/" } = Astro.props;
---

<SectionBase
  title="Le programme en un coup d'oeil"
  intro={intro}
  actionLabel="Tout afficher"
  actionHref={actionHref}
  sectionId="programme"
  sectionClass="section--courses"
>
  <div class="courses__grid">
    {
      courses.map((item) => (
        <CourseCard
          title={item.title}
          description={item.description}
          imageSrc={item.imageSrc}
          imageAlt={item.imageAlt}
          href={item.href}
        />
      ))
    }
  </div>
</SectionBase>
